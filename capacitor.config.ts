import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hrsuser.app',
  appName: 'user-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
